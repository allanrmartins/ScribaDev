name: tests

on: [push, pull_request]

# cancela um run anterior ainda em andamento no mesmo ref (economiza minutos)
concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    # Windows = plataforma principal (captura WASAPI, toasts, detecção por registro)
    runs-on: windows-latest
    timeout-minutes: 30   # suíte leva ~8 min; runner pendurado não fica 6 h preso
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"   # piso do pyproject (requires-python >=3.12)
      - name: Instalar deps do projeto (PySide6 já é core desde o corte #53; sem cuda/diarization)
        run: pip install -e .
      - name: Rodar a suite de testes
        # QT_QPA_PLATFORM=offscreen: o smoke da UI Qt roda sem display no runner
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m unittest discover -s tests -v

  test-macos:
    # Port macOS (épico #104, docs/port-mac.md): runner arm64, mesmo contrato do
    # test-linux. Sem brew install ffmpeg de partida — nenhum teste exige o binário
    # (ffmpeg_status só consulta o PATH).
    runs-on: macos-latest
    timeout-minutes: 30   # a suíte leva ~6 min; um job pendurado (viu-se 1x no #193) não segura o PR
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Instalar deps do projeto
        run: pip install -e .
      - name: Rodar a suite de testes
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m unittest discover -s tests -v

  test-linux:
    # Port multiplataforma (épico #104): trava regressões de portabilidade a cada
    # push. Os markers do pyproject deixam pyaudiowpatch/windows-toasts de fora;
    # os testes Windows-only pulam por skipUnless(sys.platform == "win32").
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Bibliotecas de sistema (Qt offscreen precisa da libegl; ffmpeg p/ áudio)
        run: sudo apt-get update && sudo apt-get install -y libegl1 ffmpeg
      - name: Instalar deps do projeto
        run: pip install -e .
      - name: Rodar a suite de testes
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m unittest discover -s tests -v
